{
  "name": "pet-mbti",
  "version": "1.0.0",
  "private": true,
  "description": "零依赖的宠物 MBTI 静态测试页",
  "scripts": {
    "test": "node --test",
    "check": "npm test && node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "qa": "node scripts/browser-qa.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
